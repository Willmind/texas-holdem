## Texas Hold'em（德州扑克）

一个基于 **Vue 3 + TypeScript + Vite** 的德州扑克项目，使用 **Pinia** 做状态编排；核心规则/结算/胜率等逻辑放在纯 TS 引擎层，方便测试与复用。

<img width="1298" height="904" alt="image" src="https://github.com/user-attachments/assets/40396ac4-c8a3-447d-b894-73a3cf019616" />

## 功能一览

- **标准流程**：盲注、翻牌/转牌/河牌、摊牌结算、边池分配
- **座位标识**：每手显示 **庄 / 小盲 / 大盲**
- **本手结果**：每局结束后右侧栏会**自动弹出**本手结果弹框（可关闭），支持查看每位玩家状态/牌型与边池分配
- **赢家高亮**：每局结束后，赢家玩家卡片会出现一圈**黄色边框**
- **胜率展示**：展示“摊牌胜率（对手随机·抽样）”（含义见下）
- **淘汰提示**：
  - 你筹码为 0 且进入 `end`：桌面底部出现不遮挡的提示条
  - 你筹码为 0 仍点击发牌：弹出“需要重新开始”的弹框

## 快速开始

```bash
npm install
npm run dev
```

然后在浏览器打开终端里提示的本地地址。

## 操作说明

- **右侧操作栏**：
  - 弃牌 / 跟注（或过牌）/ 全下
  - 滑条选择“加注到 X”后点“确认加注”
  - 发下一手 / 重置
  - **本手结果**（每局结束自动弹出，也可手动打开）
- **胜率**：
  - 仅在你仍在牌局且手牌已发时显示
  - “摊牌胜率（对手随机·抽样）”表示“如果最终摊牌，你赢的概率估计”，不等同于直播里那种“已知双方底牌”的精确值

## 常用命令

```bash
# 本地开发（Vite）
npm run dev

# 单测（Vitest）
npm test

# 类型检查 + 构建
npm run build

# 预览构建产物
npm run preview
```

## 目录结构（关键部分）

- **`src/engine/`**：纯 TypeScript 业务引擎（不依赖 Vue/Pinia/DOM）
  - **`src/engine/game.ts`**：牌局流程/状态机相关核心逻辑
  - **`src/engine/sidePot.ts`**：分池逻辑
  - **`src/engine/handEval.ts`**：牌型评估
  - **`src/engine/equity.ts`**：胜率/权益计算
  - **`src/engine/__tests__/`**：引擎单元测试
- **`src/stores/`**：Pinia 状态编排与副作用（如 AI 回合、胜率重算等）
- **`src/components/`**：纯展示组件与交互触发（不直接改引擎内部状态）

## 代码约定（分层边界）

为了保证可维护性与可测试性：

- **引擎层（`src/engine/**`）**：保持纯函数/纯 TS 逻辑，避免耦合 UI 与状态管理。
- **Store 层（`src/stores/**`）**：负责把 UI 事件映射为引擎调用，并处理异步/副作用。
- **组件层（`src/components/**`）**：只负责展示与用户交互，不直接“伸手”修改引擎内部状态。

## 贡献与验证

涉及 **规则/结算/分池/行动顺序** 等引擎改动时，务必同时：

- **补充/更新 Vitest 测试**（通常在 `src/engine/__tests__/`）
- **运行 `npm test` 与 `npm run build`**，两者通过才算完成

## 胜率（equity）说明

项目当前显示的是 **摊牌胜率（对手随机·抽样）**：

- **包含**：已知公共牌固定、未知公共牌用抽样补全后的赢/平/负概率
- **不包含**：下注导致弃牌（诈唬/价值下注）的博弈因素、对手范围（range）建模

更适合做“强弱参考”，不是“最终一定能赢底池的概率”。

## 部署（Netlify Drop）

```bash
npm install
npm run build
```

然后把 `dist/` 文件夹拖拽到 Netlify Drop 页面即可完成部署。
