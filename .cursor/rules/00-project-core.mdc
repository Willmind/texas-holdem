---
description: 项目通用规范（结构、命名、改动边界、验证）
alwaysApply: true
---

# 项目通用规范

- **最小改动原则**：优先在现有结构内修复/扩展，不做无关重构。
- **边界清晰**：
  - `src/engine/**`：纯 TS 业务引擎（不依赖 Vue/Pinia/DOM）。
  - `src/stores/**`：Pinia 状态编排与副作用（AI 回合、胜率重算等）。
  - `src/components/**`：纯展示与交互触发（不直接改引擎内部状态）。
- **命名与类型**：
  - TypeScript 必须显式类型边界（公开函数/返回值/关键结构）。
  - 避免 `any`；需要时用窄化与联合类型。
- **验证要求**：涉及引擎/规则/结算的改动，必须同时：
  - 更新或新增测试（Vitest）
  - 跑 `npm test` 与 `npm run build`（通过后才算完成）
- **不要破坏可玩性**：如果改动可能影响流程（行动顺序、下注、分池），必须补一条最小手工验证步骤。

